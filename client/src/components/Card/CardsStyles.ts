import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  items: {
    marginLeft: 20,
    marginRight: 20,
    marginBottom: 50,
  },
  filter: {
    display: 'flex',
    flexDirection: 'row',
    marginLeft: 20,
    marginRight: 20,
    marginBottom: 5
  }
});

export default styles